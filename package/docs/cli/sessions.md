---
summary: "CLI reference for `hexos sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `hexos sessions`

List stored conversation sessions.

```bash
hexos sessions
hexos sessions --active 120
hexos sessions --json
```

