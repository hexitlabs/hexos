---
summary: "CLI reference for `hexos uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `hexos uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
hexos uninstall
hexos uninstall --all --yes
hexos uninstall --dry-run
```

